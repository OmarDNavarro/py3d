# constants.py

import numpy as np

# Window
WIDTH, HEIGHT = 1920, 1080
HALF_WIDTH, HALF_HEIGHT = WIDTH / 2, HEIGHT / 2

# Camera
CAMERA_SPEED = 0.1

# Rotation
ROTATION_SPEED = 0.03
MAX_ROTATION = 2 * np.pi

# Colors
WHITE= (255, 255, 255) 
BLACK = (0, 0, 0)